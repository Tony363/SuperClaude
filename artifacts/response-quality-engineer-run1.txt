## Summary

I've created a comprehensive pytest test suite for a Calculator class with **81 tests** achieving **100% code coverage**.

### Files Created

1. **`examples/calculator/calculator.py`** - Calculator class implementation
2. **`examples/calculator/test_calculator.py`** - Comprehensive test suite

### Test Coverage

| Test Class | Tests | Coverage |
|------------|-------|----------|
| `TestAdd` | 9 | Happy path + edge cases |
| `TestSubtract` | 8 | Happy path + edge cases |
| `TestMultiply` | 10 | Happy path + edge cases |
| `TestDivide` | 15 | Happy path + error handling |
| `TestParametrized` | 25 | Batch validation |
| `TestSpecialValues` | 4 | Infinity, NaN |
| `TestMathematicalProperties` | 7 | Mathematical invariants |
| `TestReturnTypes` | 3 | Type verification |

### Key Test Patterns Used

**AAA Pattern (Arrange-Act-Assert):**
```python
def test_add_positive_integers(self, calc):
    # Arrange
    a, b = 2, 3
    # Act
    result = calc.add(a, b)
    # Assert
    assert result == 5
```

**Parametrized Tests for batch validation:**
```python
@pytest.mark.parametrize("a,b,expected", [
    (1, 1, 2),
    (0, 0, 0),
    (-1, 1, 0),
    (100, 200, 300),
])
def test_add_parametrized(self, calc, a, b, expected):
    assert calc.add(a, b) == expected
```

**Exception Testing:**
```python
def test_divide_by_zero_raises_error(self, calc):
    with pytest.raises(ZeroDivisionError) as exc_info:
        calc.divide(10, 0)
    assert "Cannot divide by zero" in str(exc_info.value)
```

**Float Precision Testing:**
```python
def test_add_small_floats(self, calc):
    result = calc.add(0.1, 0.2)
    assert math.isclose(result, 0.3, rel_tol=1e-9)
```

### Edge Cases Covered

- Zero operations (identity property)
- Negative numbers
- Mixed signs
- Large numbers (10^15)
- Float precision (0.1 + 0.2)
- Division by zero (raises `ZeroDivisionError`)
- Special values: `inf`, `-inf`, `NaN`
- Mathematical properties: commutative, associative, distributive